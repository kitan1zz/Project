# ОПИСАНИЕ ПРОЕКТА

## 1. ПРОЕКТ OVERVIEW

Образовательный веб-сайт о блокчейне, NFT и экосистеме TON, созданный для людей без технического бэкграунда, особенно для школьников. Проект решает проблему сложности понимания криптовалютных технологий через визуализации, анимации и интерактивные демонстрации. Сайт объясняет базовые концепции блокчейна, работу NFT, процесс передачи владения (включая off-chain логику) и связь между TON и Telegram простым и понятным языком.

## 2. ФИНАЛЬНАЯ СТРУКТУРА ПРОЕКТА

```
ton-blockchain-education/
├── app/                          # Next.js App Router - основная директория приложения
│   ├── api/                     # API маршруты (бэкенд)
│   │   └── nft/
│   │       └── route.ts         # API для симуляции NFT транзакций (GET, POST, DELETE)
│   ├── blockchain/              # Страница "Блокчейн"
│   │   └── page.tsx             # Объяснение работы блокчейна с визуализацией
│   ├── nft/                     # Страница "NFT"
│   │   └── page.tsx             # Объяснение NFT и передачи владения
│   ├── ton/                     # Страница "TON"
│   │   └── page.tsx             # Информация о TON и интеграции с Telegram
│   ├── demo/                    # Страница "Демо"
│   │   └── page.tsx             # Интерактивная демонстрация передачи NFT
│   ├── about/                   # Страница "О проекте"
│   │   └── page.tsx             # Информация о проекте и технологиях
│   ├── layout.tsx               # Корневой layout с навигацией и провайдером темы
│   ├── page.tsx                 # Главная страница с карточками разделов
│   └── globals.css              # Глобальные стили и Tailwind CSS
│
├── components/                   # Переиспользуемые React компоненты
│   ├── Navigation.tsx           # Навигационное меню с активными ссылками и переключением темы
│   ├── ThemeProvider.tsx        # Провайдер для управления светлой/тёмной темой
│   └── BlockVisualization.tsx   # Компонент визуализации цепочки блоков блокчейна
│
├── public/                      # Статические файлы (изображения, иконки, медиа)
│
├── package.json                 # Зависимости проекта и npm скрипты
├── tsconfig.json                # Конфигурация TypeScript
├── tailwind.config.js           # Конфигурация Tailwind CSS с кастомными цветами и анимациями
├── next.config.js               # Конфигурация Next.js
├── postcss.config.js            # Конфигурация PostCSS для Tailwind
├── vercel.json                  # Конфигурация для деплоя на Vercel
├── .eslintrc.json               # Конфигурация ESLint
├── .gitignore                   # Игнорируемые файлы Git
├── README.md                    # Документация проекта на русском языке
└── PROJECT_OVERVIEW.md           # Подробный обзор проекта
```

### Описание основных директорий:

- **app/** — содержит все страницы и маршруты приложения, используя Next.js App Router. Каждая поддиректория представляет отдельную страницу. API маршруты находятся в `app/api/`.
- **components/** — переиспользуемые React компоненты, включая навигацию, провайдер темы и визуализации блокчейна.
- **app/api/** — серверные API маршруты для симуляции работы с NFT (без реального блокчейна, in-memory storage).
- **public/** — статические файлы, доступные напрямую по URL (изображения, иконки).

## 3. FRONTEND

### Ключевые страницы:

#### Главная страница (`app/page.tsx`)
Приветственный экран с карточками разделов. Использует Framer Motion для анимации появления элементов. Градиентный дизайн с поддержкой тёмной темы. Адаптивная сетка карточек (1 колонка на мобильных, 2 на десктопе).

**Пример кода анимированной секции:**
```tsx
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.2,
    },
  },
}

const itemVariants = {
  hidden: { y: 20, opacity: 0 },
  visible: {
    y: 0,
    opacity: 1,
    transition: {
      duration: 0.5,
    },
  },
}
```

#### Страница "Блокчейн" (`app/blockchain/page.tsx`)
Объяснение работы блокчейна простым языком. Интерактивная визуализация цепочки блоков через компонент `BlockVisualization`. Анимация подсветки блоков при клике на кнопку. Описание ключевых особенностей: безопасность, децентрализация, неизменяемость, прозрачность.

#### Страница "NFT" (`app/nft/page.tsx`)
Объяснение концепции NFT через аналогии (сертификат подлинности для цифровых активов). Структура NFT (ID, владелец, метаданные, история транзакций). Пошаговое описание процесса передачи владения (4 шага). Важное примечание об off-chain хранении файлов.

#### Страница "TON" (`app/ton/page.tsx`)
Информация о TON (The Open Network) и его связи с Telegram. Объяснение преимуществ интеграции. Пошаговый процесс работы вместе (4 шага). Визуальные карточки с ключевыми особенностями.

#### Страница "Демо" (`app/demo/page.tsx`)
Интерактивная симуляция передачи NFT. Выбор NFT и получателя через UI. Визуализация транзакций в реальном времени с анимациями статусов (pending → confirmed). История транзакций с возможностью сброса.

#### Страница "О проекте" (`app/about/page.tsx`)
Цели и задачи проекта. Используемые технологии (Frontend и Backend). Особенности реализации.

### Переиспользуемый UI компонент:

**BlockVisualization** (`components/BlockVisualization.tsx`) — компонент для визуализации цепочки блоков:
- Принимает массив блоков и опциональный ID подсвечиваемого блока
- Анимация появления блоков с задержкой через Framer Motion
- Подсветка активного блока с изменением масштаба
- Визуальная связь между блоками через иконки стрелок
- Адаптивный дизайн с поддержкой тёмной темы

**Пример использования:**
```tsx
<BlockVisualization 
  blocks={blocks} 
  highlightBlock={highlightBlock} 
/>
```

### Анимации, помогающие пониманию:

1. **Последовательное появление элементов** — на главной странице карточки появляются одна за другой
2. **Подсветка блоков** — на странице блокчейна блоки подсвечиваются по очереди, показывая связь
3. **Анимация транзакций** — на странице демо транзакции появляются с анимацией, статусы меняются плавно
4. **Hover эффекты** — интерактивные элементы реагируют на наведение
5. **Переходы между страницами** — плавные переходы при навигации

## 4. BACKEND (МИНИМАЛЬНЫЙ)

### Описание логики:

Backend реализован через Next.js API Routes и использует in-memory storage (данные хранятся в памяти сервера, не в базе данных). Это упрощённая симуляция для демонстрации работы с NFT без реального блокчейна. При перезапуске сервера данные сбрасываются.

### Пример API маршрута:

**Файл:** `app/api/nft/route.ts`

```typescript
// In-memory storage для демонстрации
let nfts = [
  { id: '1', name: 'Цифровая картина #1', owner: 'Алиса', image: '🎨' },
  { id: '2', name: 'Виртуальный дом', owner: 'Боб', image: '🏠' },
  { id: '3', name: 'Коллекционная карта', owner: 'Чарли', image: '🃏' },
]

let transactions: Array<{
  id: string
  from: string
  to: string
  nftId: string
  status: 'pending' | 'confirmed' | 'failed'
  timestamp: number
}> = []

// GET - получить все NFT и транзакции
export async function GET() {
  return NextResponse.json({ nfts, transactions })
}

// POST - создать транзакцию передачи NFT
export async function POST(request: NextRequest) {
  const body = await request.json()
  const { nftId, from, to } = body

  // Валидация
  const nft = nfts.find((n) => n.id === nftId)
  if (!nft || nft.owner !== from) {
    return NextResponse.json({ error: 'Ошибка валидации' }, { status: 403 })
  }

  // Создание транзакции
  const transaction = {
    id: `tx_${Date.now()}`,
    from,
    to,
    nftId,
    status: 'pending' as const,
    timestamp: Date.now(),
  }

  transactions.unshift(transaction)

  // Симуляция обработки
  await new Promise((resolve) => setTimeout(resolve, 1000))

  // Обновление владельца
  nfts = nfts.map((n) => (n.id === nftId ? { ...n, owner: to } : n))
  transaction.status = 'confirmed'

  return NextResponse.json({ success: true, transaction, nfts })
}

// DELETE - сброс демо-данных
export async function DELETE() {
  nfts = [/* начальные данные */]
  transactions = []
  return NextResponse.json({ success: true })
}
```

### Симуляция NFT владения/передачи:

1. **Хранение данных**: Массивы `nfts` и `transactions` в памяти сервера (переменные уровня модуля)
2. **Создание транзакции**: При POST-запросе создаётся объект транзакции со статусом "pending"
3. **Валидация**: Проверка, что отправитель является текущим владельцем NFT
4. **Обработка**: Симуляция задержки сети (1 секунда через `setTimeout`)
5. **Обновление**: Изменение владельца NFT и статуса транзакции на "confirmed"
6. **Возврат данных**: Клиент получает обновлённые списки NFT и транзакций

**Важно**: Это демонстрация. В реальном блокчейне процесс включает:
- Подпись транзакции приватным ключом владельца
- Отправку в сеть блокчейна (p2p сеть)
- Валидацию узлами сети (майнерами/валидаторами)
- Запись в блокчейн после подтверждения (consensus)
- Неизменяемость после записи

## 5. ИНСТРУКЦИИ ПО ЗАПУСКУ И ДЕПЛОЮ

### Как запустить локально:

1. **Убедитесь, что установлен Node.js 18+**
   ```bash
   node --version
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```

4. **Откройте браузер:**
   ```
   http://localhost:3000
   ```

5. **Для сборки продакшен версии:**
   ```bash
   npm run build
   npm start
   ```

### Как задеплоить на Vercel:

#### Вариант 1: Через Vercel CLI (рекомендуется)

1. **Установите Vercel CLI глобально:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в аккаунт Vercel:**
   ```bash
   vercel login
   ```

3. **В директории проекта выполните:**
   ```bash
   vercel
   ```

4. **Следуйте инструкциям в терминале:**
   - Выберите проект (или создайте новый)
   - Подтвердите настройки
   - Дождитесь деплоя

5. **После деплоя сайт будет доступен по адресу:**
   ```
   https://your-project.vercel.app
   ```

#### Вариант 2: Через веб-интерфейс Vercel

1. **Зайдите на [vercel.com](https://vercel.com)** и войдите в аккаунт (можно через GitHub)

2. **Нажмите "Add New Project"** или "Import Project"

3. **Импортируйте ваш Git-репозиторий:**
   - Подключите GitHub/GitLab/Bitbucket
   - Выберите репозиторий с проектом

4. **Vercel автоматически определит Next.js:**
   - Framework Preset: Next.js
   - Build Command: `npm run build`
   - Output Directory: `.next`
   - Install Command: `npm install`

5. **Нажмите "Deploy"**

6. **Дождитесь завершения деплоя** (обычно 1-2 минуты)

7. **Сайт будет доступен по адресу:**
   ```
   https://your-project.vercel.app
   ```

### Переменные окружения:

Для этого проекта **не требуются** переменные окружения, так как используется in-memory storage для демонстрации. Если в будущем понадобится подключить реальную базу данных или API ключи, их можно добавить в настройках проекта на Vercel.

### Docker (опционально):

Docker **не обязателен** для этого проекта, так как Vercel предоставляет встроенную поддержку Next.js и автоматически собирает и деплоит проект. Если нужен Docker для других целей (например, для локальной разработки в контейнере), можно создать `Dockerfile`, но это не требуется для стандартного деплоя на Vercel.

---

## ЗАКЛЮЧЕНИЕ

Проект полностью готов к использованию и деплою. Все файлы созданы, код написан на TypeScript с использованием современных практик разработки. Сайт адаптивен, поддерживает тёмную тему, содержит интерактивные демонстрации и готов к развёртыванию на Vercel.

